# GraphQL Explorer for Dev GraphQL

## Get Started

You can develop it locally with the following commands:

```
> git clone git@github.com:dev-protocol/graphql-explorer.git
> cd graphql-explorer
> yarn
> yarn start
```
